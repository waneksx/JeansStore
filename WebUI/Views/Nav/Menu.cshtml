@model  IEnumerable<string>

@Html.ActionLink("Home", "Index", "Jeans")
@foreach (var link in Model)
{
    @Html.RouteLink(link, new
{
    controller = "Jeans",
    action = "Index",
    color = link,
    page = 1
},
new
{
    @class = link == ViewBag.SelectedColor ? "selected" : null
})
}

